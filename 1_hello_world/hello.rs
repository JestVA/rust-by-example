fn main() {
    // println!("Hello World!");
    // println!("I'm a Rustacean!");

    /*
        comment block
        /// Generate library docs for the following item
        //! Generate library docs for the enclosing item
    */

    let x = 5 + 90 + 5;
    println!("Is `x` 10 or 100? x = {}", x); // 100 now!
}
